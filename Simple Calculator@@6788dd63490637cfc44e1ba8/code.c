#include <stdio.h>
int main() {
    int x,y,s;
    char z;
    scanf("%d %d",&d &d);
    if(x=="+"){
        s=x+y;
        printf("%d",s);
    }
    else if(x=="-"){
        s=x-y;
        printf("%d",s);
    }
    else if(x=="*"){
        s=x*y;
        printf("%d",s);
    }
    else{
        s=x/y;
        printf("%d",s);
    }
    return 0;
}